import { defineConfig } from 'windicss/helpers'

export default defineConfig({
  shortcuts: {
    'glass-card-color':'bg-purple-400 rounded-md bg-clip-padding backdrop-filter backdrop-blur-none bg-opacity-30 border',

    // utils
    'flex-center': 'items-center justify-center',
  },
})
